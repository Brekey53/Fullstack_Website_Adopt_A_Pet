{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  //A Api aceita pedidos de qualquer host 
  "AllowedHosts": "*",

  "ConnectionStrings": {
    // ligar ao MYSQL - localhost, na porta 3306 - onde corre o script no docker, nome da bd e username/palavrapasse
    "DefaultConnection": "server=localhost;port=3306;database=croae_projeto;user=root;password=root",
    "Redis": "localhost:6379" // Assumindo que o Redis está a correr em localhost
  },
  // para a autenticação JWT. a key é usada para assinar os tokens, o issuer é quem emite e o audience é quem utiliza
  "JwtSettings": {
    "Key": "oMEUchAPeuTem3Bicostem3BIcosOmEuChapeu",
    "Issuer": "api",
    "Audience": "api"
  }
}
